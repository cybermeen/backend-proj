$kind: http-request
name: Login
url: http://localhost:3000/auth/login
method: POST
headers:
  x-api-key: "{{api_key}}"
body:
  type: json
  content: "{\r

    \  \"username\": \"za_dev\",\r

    \  \"password\": \"SecurePass123!\"\r

    }"
auth:
  type: bearer
  credentials:
    token: "{{access_token}}"
scripts:
  - type: afterResponse
    code: "const response = pm.response.json();\r

      if (response.token) {\r

      \  pm.collectionVariables.set('access_token', response.token);\r

      }"
    language: text/javascript
order: 565150178251754
