$kind: http-request
name: Get inventory summary
url: http://localhost:3000/reports/inventory/summary
method: GET
headers:
  x-api-key: "{{api_key}}"
auth:
  type: bearer
  credentials:
    token: "{{access_token}}"
order: 4503587217345631
